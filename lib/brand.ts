/**
 * App branding — configurable via env so the product name is not hardcoded.
 * Set NEXT_PUBLIC_APP_NAME in .env.local to rename the whole app.
 */
export const APP_NAME = process.env.NEXT_PUBLIC_APP_NAME || 'ChurnGuard'
export const APP_TAGLINE = 'Customer Retention for Paytm Business'
